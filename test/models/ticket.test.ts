// import * as t from '../../src/models/ticket'

afterEach(() => {
    jest.clearAllMocks();
});

describe('Ticket', () => {
    it('description', (done) => {
        expect(true).toEqual(true);
        done();
    });
});
